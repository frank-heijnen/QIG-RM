# Python generated files
__pycache__/
*.py[cod]
*$py.class

# Conda environment folders to ignore, right now not needed

# Holdings (contains private broker data / screenshots)
holdings/

# Per-run backtest outputs (regenerable; configs in-repo are enough)
results/
